# Example only - do NOT commit real secrets. Create your own with:
#   kubectl create secret generic portfolio-db-secret \
#     --namespace portfolio-app \
#     --from-literal=DB_USERNAME=portfolio_user \
#     --from-literal=DB_PASSWORD=portfolio_pass
apiVersion: v1
kind: Secret
metadata:
  name: portfolio-db-secret
  namespace: portfolio-app
type: Opaque
stringData:
  DB_USERNAME: portfolio_user
  DB_PASSWORD: portfolio_pass
